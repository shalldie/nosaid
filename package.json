{
  "name": "nosaid",
  "version": "1.0.0",
  "private": true,
  "description": "一些工具包",
  "scripts": {
    "clean": "rm -rf node_modules **/*/node_modules",
    "build": "pnpm -r build",
    "patch": "pnpm -r exec npm version patch --no-git-tag-version",
    "pub": "pnpm -r publish"
  },
  "license": "MIT",
  "devDependencies": {
    "prettier": "^2.8.4"
  }
}
